load("@npm//:defs.bzl", "npm_link_all_packages")
npm_link_all_packages(name = "node_modules")

exports_files([
    "package.json",
    "pnpm-lock.yaml",
    ".npmrc",
])

test_suite(
    name = "test",
    tests = [
        "//pkg/pad-surround:test",
        "//pkg/consumes-ps:test",
    ],
)
